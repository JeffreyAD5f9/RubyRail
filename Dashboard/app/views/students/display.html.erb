<a href="/dojos/<%= @dojo.id %>/students/new">Create New</a>
<a href="/dojos/<%= @dojo.id %>/students">Back</a>


<table>
  <tr>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Email</th>
    <th>Actions</th>
  </tr>
    <tr>
      <td><%= @student.first_name %></td>
      <td><%= @student.last_name %></td>
      <td><%= @student.email %></td>
      <td><a href = "/dojos/<%= @dojo.id %>/students/<%= @student.id %>/edit">Edit</a></td>

    </tr>
</table>
